// Admin identification is now handled via Firebase custom claims (`admin: true`).
// Avoid hard-coded admin emails in code. To set admin users, set a custom
// claim `admin=true` via the Firebase Admin SDK for the desired uid.

// Deprecated placeholder kept for backward compatibility (do not use).
const String kAdminEmail = '';
